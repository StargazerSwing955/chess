package chess;

public class PawnMoveCal {

    public ChessMove pawnMove(ChessPosition pos, ChessBoard board){
        // same col row+1/-1

        //if piece in diagonal, move there

        //promotion
       ChessMove placeHolder =  (pos, pos, null);
       return placeHolder;
    }


}
